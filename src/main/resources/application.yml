
security:
  jwt:
    token:
      secret-key: ${JWT_SECRET}

server:
  port: 9090
  error:
    include-stacktrace: never

spring:
  application:
    name: authentication

  datasource:
    url: jdbc:mysql://localhost:3306/teleport_db
    username: root
    password: kheder
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


